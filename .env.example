
# Example .env file for PortnoxMCP
PORTNOXAPIKEY=your-portnox-api-key-here
ASPNETCORE_ENVIRONMENT=Development

# Port for the MCP HTTP server (default: 8080)
MCP_HTTP_PORT=8080

# Idle timeout (in seconds) for MCP HTTPTransport. Use -1 for infinite (default).
MCP_HTTP_IDLE_TIMEOUT_SECONDS=-1
